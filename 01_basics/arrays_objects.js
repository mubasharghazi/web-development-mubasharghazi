let arr = [1, 2, 3];
arr.push(4); // [1, 2, 3, 4]
console.log(arr[0]); // 1

let person = {
  name: "Ghazi",
  age: 30
};
console.log(person.name); // "Ghazi"

// Literals Example
let user = "Mubashar";
let message = `Welcome, ${user}!`;
console.log(message); // Welcome, Mubashar!